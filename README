Simple script for parsing public VPNGate VPN lists.


Usage:
	
	vpngate [-h] [-l] [-u] [-b] [-d HOSTNAME] [-s HOSTNAME]

	optional arguments:
	  -h, --help   show this help message and exit
	  -l           list cached VPN servers
	  -u           update VPN list cache
	  -b           show VPNs with lowest ping
	  --save HOSTNAME  save OpenVPN config for given server
	  --show HOSTNAME  show more information about specified host


Known problems:

- Can't connect to VPN for now

