Simple script for parsing public VPNGate VPN lists.


Usage:
	
	list-vpn [-h] [-u] [-l] [-s HOSTNAME] [-c HOSTNAME]

	-h, --help          show this help message and exit
	-u, --update-cache  update VPN list cache                  
	-l, --list          list VPN servers                       
	-s HOSTNAME         save OpenVPN config for given server                                                  
	-c HOSTNAME         connect to known VPN server using its hostname


How to install:

mkdir /usr/local/bin/list-vpn-src &> /dev/null
cp -f ./list-vpn.py /usr/local/bin/list-vpn-src/list-vpn.py
cp -f ./nmcli_util.py /usr/local/bin/list-vpn-src/nmcli_util.py
cp -f ./vpn_util.py /usr/local/bin/list-vpn-src/vpn_util.py
cp -f ./util.py /usr/local/bin/list-vpn-src/util.py
cp -f ./list-vpn /usr/local/bin/list-vpn
chmod +x /usr/local/bin/list-vpn

then run 'list-vpn -h'


Known problems:

- The scipt can't really detect whether 'nmcli' has failed or not
